server:
  port: 8083

spring:
  application:
    name: Cards
  datasource:
    url: jdbc:h2:mem:cardsDB
    username: sa
    password: ""
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

